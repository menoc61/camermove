# Phase 07 Plan 01: API Parcels — Tarif, Tracking, Exports

## Frontmatter
```yaml
---
phase: 07
plan: 01
wave: 1
depends_on: [06-vague-a-hebergement-mobilite]
theme: transport hero preserved
animation: none
files_modified:
  - packages/db/prisma/schema.prisma # Parcel/ParcelStatusLog already correct, no migration needed beyond Payment nullable
  - apps/api/src/parcels/routes.ts
  - apps/api/src/parcels/schema.ts # new (extract Zod)
  - apps/api/src/parcels/service.ts # new
  - apps/api/src/parcels/repository.ts # new
  - apps/api/src/lib/export.ts
  - packages/shared/src/money.ts # parcel pricing helper if needed
files_created:
  - apps/api/src/parcels/schema.ts
  - apps/api/src/parcels/service.ts
  - apps/api/src/parcels/repository.ts
  - apps/api/src/parcels/service.test.ts
autonomous: true
requirements:
  - PARCEL-01..04 (création tarif, paiement, suivi 6 états, traçabilité)
  - AGENTS §1 ACID, §2 metadata, §6 exportable
must_haves:
  truths:
    - POST /parcels calcule shippingCost via AppSettings grilles (base 500 + 100*kg + par type, configurable sans redeploy) + trackingNumber CM-*, crée statusHistory registered
    - POST /parcels est idempotent Idempotency-Key, log req.meta, AuditLog, Kafka parcel.created, invalidateCache parcels*
    - GET /parcels?dateFrom&dateTo&q&page/limit (owner) + GET /parcels/:id owner/admin + GET /parcels/track/:trackingNumber public sanitized (pas de PII complet)
    - PATCH /admin/parcels/:id/status FSM registered→picked_up→in_transit→arrived→available_for_pickup→delivered, admin only, AuditLog
    - GET /parcels/export + GET /admin/parcels/export csv SEARCH_MAX_LIMIT Content-Disposition
    - POST /parcels/:id/pay polymorphe via Payment.bookingId null
  prohibitions:
    - statement: No PII leak on public track — track/:trackingNumber must not return recipientPhone full if not owner
      status: active
      verification: unit track sanitized shape
---
```

## Context
Parcels est minimal stub `parcels/routes.ts:1` avec `statusLogs` au lieu de `statusHistory:528`, mapping faux `originCity→senderCity`, `totalAmount→shippingCost`, `weight→weightKg`, pas de tarif grille, pas d'export, pas d'idempotency/meta/audit/kafka, pas de FSM admin, pas de sanitized track. `Payment parcel` back-relation déjà ajoutée `schema.prisma:276`.

## Tasks

### Task 1 — Repository + Service Tarif & FSM
**files:** `apps/api/src/parcels/repository.ts`, `apps/api/src/parcels/service.ts`
**read_first:** `apps/api/src/hotels/service.ts:47` (ACID + AppSettings 30s), `admin/service.ts:242` (audit), `packages/db/prisma/schema.prisma:503` (ParcelStatus enum)
<acceptance_criteria>
- `repository.ts` `buildParcelWhere` (userId, recipientCity, status, q sender/recipient, dateFrom/dateTo) + `findParcels/countParcels/findParcelById/findParcelByTrackingNumber` avec indexes `@@index([userId,status]):532`
- `service.ts` `calcShippingCost({parcelType,weightKg,declaredValue,origin,dest})` lit `AppSettings.featureFlags.parcelPricing ?? {base:500, perKg:100, perType:{default:0}}` cached 30s via `getAppSettingsCached` (copier pattern `payments/commission.ts:30`), `createParcel({senderName,phone,recipientName,phone,senderCity,recipientCity,parcelType,weightKg,dimensionsCm,description,declaredValue,userId})` fait `prisma.$transaction` : `trackingNumber CM-${random}`, `shippingCost`, crée `Parcel` + `ParcelStatusLog {status:registered, note:"Colis enregistré"}` + `AuditLog` + `parcel.created` Kafka + `invalidateCache parcels*`
- `service.ts` `advanceParcelStatus({parcelId,actorId,role,nextStatus,location,note})` valide FSM `registered→picked_up→in_transit→arrived→available_for_pickup→delivered` (enum `ParcelStatus:319`), `admin|super_admin` only, crée `ParcelStatusLog`, update `Parcel.status/currentLocation`, audit, kafka `parcel.status.updated`, invalidateCache
- `getHold?` non applicable (pas de sièges), mais `paymentId` polymorphe
</acceptance_criteria>
<action>
1. Créer repository where builder
2. Implémenter calcShippingCost + createParcel + advanceParcelStatus avec FSM whitelist + audit/kafka/cache
3. Ajouter `createParcelPayment` polymorphe (Payment bookingId null + Parcel.paymentId)
</action>
<verify>unit calcShippingCost base+perKg, FSM valid→invalid 400, transaction trackingNumber unique</verify>

### Task 2 — Routes Zod + RBAC + Meta + Idempotency + Export
**files:** `apps/api/src/parcels/routes.ts`, `apps/api/src/parcels/schema.ts`
**read_first:** `apps/api/src/hotels/routes.ts:1` (cache/meta), `search/routes.ts:12` (cacheKey)
<acceptance_criteria>
- `schema.ts` unique source : `CreateParcelSchema {senderName 2+,senderPhone 6+,recipientName 2+,recipientPhone 6+,senderCity 2+,recipientCity 2+,parcelType 1+,weightKg Decimal? 0.1..1000?,dimensionsCm 200?,description 500?,declaredValue Int?}`, `ParcelStatusUpdateSchema {status enum ParcelStatus, location 100?, note 500?}`, `ParcelSearchQuery {q?,status?,recipientCity?,dateFrom?,dateTo?,page/perPage 1..50, limit/offset, orderBy?,groupBy?}`
- `routes.ts` : `GET /parcels` `requireAuth` cache 60s `cacheKey("parcels", sorted)` + meta `parcels.list` + paginé ; `POST /parcels` `requireAuth` `Idempotency-Key` + `req.log.info({...meta,senderCity,recipientCity,parcelType,userId}, "parcels.create")` 201 ; `GET /parcels/:id` owner/admin ; `GET /parcels/track/:trackingNumber` public `sanitizeParcelForTrack(parcel)` (masque `senderPhone/recipientPhone` → `***1234`, pas de `userId`), `rateLimit` isGeneral mais documenter ; `PATCH /admin/parcels/:id/status` `requireAuth admin` FSM ; `GET /parcels/export` + `GET /admin/parcels/export` via `parseExportQuery/sendExport` `SEARCH_MAX_LIMIT`
- Tous Zod, AppError 404/409, `statusHistory` pas `statusLogs`
</acceptance_criteria>
<action>
1. Extraire schema.ts canonique
2. Réécrire routes.ts sur service/repository avec cache/meta/idempotency/export/FSM + sanitized track
3. Ajouter `POST /parcels/:id/pay` polymorphe
</action>
<verify>`POST /parcels` replay Idempotency-Key même 201, `track` sans auth retourne sanitized sans téléphones complets, typecheck 0</verify>

### Task 3 — Verification
**files:** all
<acceptance_criteria>
- `prisma validate` + `typecheck 0` (enlever // @ts-nocheck parcels après)
- `vitest parcels/service.test.ts` calc + FSM + sanitized
- `rg "statusLogs|totalAmount" apps/api/src/parcels` clean
</acceptance_criteria>

## Risks
- AppSettings.featureFlags.parcelPricing JSON non typé — fallback base 500 +100/kg si absent, ne pas bloquer MVP
- Public track sanitized doit garder `trackingNumber/status/currentLocation/statusHistory` mais masquer PII — tester

## Done When
- 4 listes + 1 création tarif + 1 FSM admin + 2 exports + pay polymorphe, cache/idempotent/audit/kafka verts
