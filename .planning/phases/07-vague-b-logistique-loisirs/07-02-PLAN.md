# Phase 07 Plan 02: API Events + QR — Catalogue, Atomique sold, Fil Pilote Admin

## Frontmatter
```yaml
---
phase: 07
plan: 02
wave: 1
depends_on: [06-vague-a-hebergement-mobilite]
theme: transport hero preserved
animation: none
files_modified:
  - apps/api/src/events/routes.ts
  - apps/api/src/events/schema.ts # new
  - apps/api/src/events/service.ts # new
  - apps/api/src/events/repository.ts # new
  - packages/db/prisma/schema.prisma # Event/TicketCategory/EventBooking already correct
  - apps/api/src/tickets/ticket.service.ts # reuse for event QR if needed
files_created:
  - apps/api/src/events/schema.ts
  - apps/api/src/events/service.ts
  - apps/api/src/events/repository.ts
  - apps/api/src/events/service.test.ts
autonomous: true
requirements:
  - EVENT-01..04 (catalogue, catégories, disponibilité, paiement, billet QR, contrôle entrée)
  - AGENTS §1 ACID (quantity-sold), §2 metadata, §6 exportable
must_haves:
  truths:
    - GET /events?search&city&eventType&dateFrom&dateTo&q&page/limit cache 60s, filtre status on_sale/limited uniquement (admin seul publie partnerStatus approved)
    - GET /events/:id incl ticketCategories (price/quantity/sold/status)
    - POST /events/bookings atomique FOR UPDATE : check quantity-sold >= qty sinon 409, inc sold, décr not availableQuantity, crée EventBooking ticketNumber EVT-* + qrCode, totalAmount price*qty
    - POST atomique est idempotent Idempotency-Key, log meta, AuditLog, Kafka event.booking.created, invalidateCache events*
    - GET /events/bookings/me + :id + /export csv SEARCH_MAX_LIMIT, admin GET /admin/events + /admin/event-bookings + exports
    - POST /events/bookings/:id/pay polymorphe + worker confirm + billet QR réutilise tickets/ticket.service generateVerificationCode + qrDataUrl, POST /tickets/verify valide event QR
  prohibitions:
    - statement: No availableQuantity — TicketCategory has quantity/sold, not availableQuantity
      status: active
      verification: rg availableQuantity apps/api --type ts must be 0 after fix
    - statement: No race on last ticket — 2 concurrents last qty must serialize 1 201 1 409
      status: active
      verification: vitest events concurrent sold
---
```

## Context
Events était race `events/routes.ts:65` `availableQuantity` inexistant `TicketCategory:597` a `quantity Int + sold Int:605`, filtre `status published`inexistant (`EventTicketStatus:345` `on_sale/limited/sold_out/cancelled`), `eventDate` vs `startDate:583`, `category` vs `eventType`, `POST` 2 queries non atomiques + `eventId` non vérifié vs `category.eventId`, pas de cache/meta/idempotency/export/audit/kafka.

## Tasks

### Task 1 — Repository + Service Atomique sold
**files:** `apps/api/src/events/repository.ts`, `apps/api/src/events/service.ts`
**read_first:** `apps/api/src/rentals/service.ts:1` (FOR UPDATE overlap), `tickets/ticket.service.ts:54` (generateVerificationCode qrDataUrl), `bookings/service.ts:66` (transaction)
<acceptance_criteria>
- `repository.ts` `buildEventWhere` (search name contains, city, eventType, dateFrom/dateTo on startDate, status on_sale/limited, partnerStatus approved) + `findEvents/countEvents/findEventById` avec indexes `@@index([city,status]):592`, `findTicketCategoryById` avec `@@index([eventId,status]):612`
- `service.ts` `createEventBooking({eventId,ticketCategoryId,userId,quantity})` : `$transaction` `SELECT FOR UPDATE TicketCategory` → `available = quantity - sold` → 409 si `available < qty` → `totalAmount = price*qty` → `ticketNumber EVT-${random}` + `qrCode = CM-T:${verificationCode}` (reuse `generateVerificationCode():tickets 21`), `qrDataUrl` via `QRCode.toDataURL` si lib qrcode dispo sinon `qrCode` seul ; crée `EventBooking status pending_payment` avec `ticketNumber/qrCode` + `TicketCategory.update sold increment qty` ; `AuditLog` + `event.booking.created` Kafka + `invalidateCache events*`
- `service.ts` `verifyEventTicket({verificationCode|ticketNumber})` pour contrôle entrée : `findFirst EventBooking where ticketNumber|qrCode = code` → retourne booking + event + category, idempotent
- `createEventBookingPayment` polymorphe (Payment bookingId null + EventBooking.paymentId, one-pending guard)
</acceptance_criteria>
<action>
1. Créer repository where builder
2. Implémenter service ACID sold + ticketNumber/qrCode + audit/kafka/cache
3. Ajouter verify helper + payment polymorphe
</action>
<verify>unit available calc, concurrent quantity=5 sold=4 qty=2 vs qty=1 → 1 succès 1 409, ticketNumber unique</verify>

### Task 2 — Routes Zod + Cache + Idempotency + Export + QR
**files:** `apps/api/src/events/routes.ts`, `apps/api/src/events/schema.ts`
**read_first:** `apps/api/src/hotels/routes.ts` (cache/meta), `tickets/validation.ts:8` (LookupQuery)
<acceptance_criteria>
- `schema.ts` unique source : `EventSearchQuery {search?,city?,eventType?,dateFrom?,dateTo?,q?,page/perPage 1..50, limit/offset, orderBy?,groupBy?}`, `CreateEventBookingSchema {eventId cuid, ticketCategoryId cuid, quantity 1..10}`, `EventBookingParams {id cuid}`
- `routes.ts` : `GET /events` cache 60s `cacheKey("events", sorted)` + `req.log.info({...meta,search,city,eventType}, "events.search")` ; `GET /events/:id` ; `POST /events/bookings` `requireAuth` `Idempotency-Key` + meta `event.booking.create` 201 ; `GET /events/bookings/me?dateFrom&dateTo&q&page/limit` + `:id` owner/admin ; `GET /events/bookings/export` + `GET /admin/events` + `GET /admin/events/export` + `GET /admin/event-bookings` + export `SEARCH_MAX_LIMIT` `Content-Disposition` ; `POST /events/bookings/:id/pay` polymorphe ; `POST /tickets/verify` déjà existant `tickets/lookup.ts:20` réutilisé pour event QR (`?code=` ou `ticketNumber`)
- Tous Zod cuid strict, AppError 404/409, admin `requireAuth admin|super_admin`
</acceptance_criteria>
<action>
1. Extraire schema.ts canonique (eventType enum EventType:336 `concert|sport|...`)
2. Réécrire routes.ts sur service/repository avec cache/meta/idempotency/export + admin
3. Vérifier `GET /events` n'utilise plus `published` ni `availableQuantity` ni `eventDate` ni `category`
</action>
<verify>`GET /events?city=Yaoundé&eventType=concert` retourne on_sale, `POST /events/bookings` last ticket concurrent 1/1, typecheck 0</verify>

### Task 3 — Verification
**files:** all
<acceptance_criteria>
- `prisma validate` + `typecheck 0` (enlever // @ts-nocheck events)
- `vitest events/service.test.ts` available + concurrent
- `rg availableQuantity|published|eventDate" apps/api/src/events` clean (0 hits)
</acceptance_criteria>

## Risks
- Admin publie seul (pas d'espace organisateur) — conformément décision user "Admin seul publie" (06) → pas de `organizer` flow à implémenter
- `TicketCategory.sold` inc sans `SELECT FOR UPDATE` = race — doit être dans tx

## Done When
- 2 listes + 1 détail + 1 création atomique sold + 3 me/:id/exports + pay polymorphe + QR verify réutilisé, cache/idempotent/audit/kafka verts
