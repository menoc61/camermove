# Phase 07 Plan 03: Web + Admin Parcels/Events — Suivi Timeline + Billetterie + Dashboard Global

## Frontmatter
```yaml
---
phase: 07
plan: 03
wave: 2
depends_on: [07-01, 07-02]
theme: transport hero 2x preserved
animation: framer-motion hoverLift + opacity
files_modified:
  - apps/web/app/page.tsx # no hero change, just counts if needed
  - apps/web/app/parcels/page.tsx
  - apps/web/app/parcels/track/[trackingNumber]/page.tsx # new
  - apps/web/app/events/page.tsx
  - apps/web/app/events/[id]/page.tsx # new
  - apps/web/app/dashboard/page.tsx
  - apps/web/components/dashboard/Dashboard.tsx
  - apps/web/components/admin/AdminShell.tsx
  - apps/web/components/admin/AdminParcels.tsx # new
  - apps/web/components/admin/AdminEvents.tsx # new
  - apps/web/lib/api/parcels.ts # new
  - apps/web/lib/api/events.ts # new
files_created:
  - apps/web/app/parcels/track/[trackingNumber]/page.tsx
  - apps/web/app/events/[id]/page.tsx
  - apps/web/lib/api/parcels.ts
  - apps/web/lib/api/events.ts
  - apps/web/components/admin/AdminParcels.tsx
  - apps/web/components/admin/AdminEvents.tsx
autonomous: true
requirements:
  - PARCEL-05/EVENT-05 (web parcours complet)
  - DASH-02 (dashboard Mes colis / Mes billets events)
  - ADMIN-05 (admin colis+events + exports)
must_haves:
  truths:
    - /parcels formulaire expéditeur/destinataire/type/poids/dimensions + tarif estimé shippingCost live + création 201 → /parcels/track/:trackingNumber timeline 6 états registered→delivered + suivi public sanitized + mes colis
    - /events catalogue (affiche, venue, city, startDate, eventType, prix) + filtres search/city/eventType/q + pagination + fiche ticketCategories (quantity-sold, prix, status) + panier qty 1..10 + POST → 201 + QR billet + pay
    - Dashboard Tabs Voyages|Hôtels|Véhicules|Colis|Billets (global) VISIBLE_LIMIT 3 chaque, empty CTAs, Promise.all 5 sources
    - Admin Parcols + Événements tables + exports dateFrom/dateTo csv + FSM/parterStatus approve + ticketCategory sold
  prohibitions:
    - statement: No PII leak on public track page — must be sanitized view
      status: active
      verification: public track page shows masked phones and no userId
    - statement: No direct DB from web — only REST /api/v1
      status: active
      verification: rg "prisma\." apps/web --type ts --type tsx must be empty except app/page.tsx counts best-effort
---
```

## Context
Web `app/parcels/page.tsx:1` et `events/page.tsx:1` sont `ComingSoon` `components/coming-soon.tsx:1`. Dashboard `Dashboard.tsx` n'a que 3 Tabs Voyages/Hôtels/Véhicules après 06-03, manque Colis/Billets. `AdminShell.tsx:19` n'a que Hôtels/Véhicules après 06-03, manque Parcels/Events. Pas de `track/[trackingNumber]` public, pas de `events/[id]` fiche billetterie. Prix colis grille `AppSettings` déjà côté API 07-01, events `on_sale/limited` 07-02.

## Tasks

### Task 1 — Pages Parcels (form + suivi)
**files:** `apps/web/lib/api/parcels.ts`, `apps/web/app/parcels/page.tsx`, `apps/web/app/parcels/track/[trackingNumber]/page.tsx`
**read_first:** `apps/web/lib/api/hotels.ts:1` (fetchHotels pattern Idempotency-Key), `app/hotels/page.tsx` (FilterBar+Pagination), `components/search/trip-card.tsx` (card)
<acceptance_criteria>
- `lib/api/parcels.ts` `fetchParcels(token,params) GET /api/v1/parcels?q&status&dateFrom&dateTo&page/limit`, `createParcel(token,body) POST /api/v1/parcels Idempotency-Key`, `trackParcel(trackingNumber) GET /api/v1/parcels/track/:trackingNumber` public no auth, `fetchParcel(id)`, `createParcelPayment`
- `app/parcels/page.tsx` client : tabs `Envoyer un colis | Mes colis | Suivi public` (shadcn Tabs). Tab Envoyer : form senderName/Phone/city + recipientName/Phone/city + parcelType Select + weightKg Input + dimensions + description + tarif estimé `base 500 +100*kg` live (calcul client identique AppSettings fallback) ; `Stepper` non nécessaire ; `POST` → `router.push(/parcels/track/${trackingNumber})` + toast. Tab Mes colis : `useQuery ["parcels",me]` table trackingNumber/sender→recipient/status/currentLocation/shippingCost + Voir suivi. Tab Suivi : Input trackingNumber + `trackParcel` → timeline 6 états `registered→picked_up→in_transit→arrived→available_for_pickup→delivered` avec `ParcelStatusLog` `location/note/createdAt`, badge status couleur `stone-900` vs `muted`, currentLocation.
- `app/parcels/track/[trackingNumber]/page.tsx` RSC ou client : `fetch(trackParcel)` public, affiche sanitized view (phones masqués `***1234` déjà côté API), timeline, pas de userId.
</acceptance_criteria>
<action>
1. Créer lib/api/parcels.ts
2. Remplacer ComingSoon par 3 tabs + timeline
3. Créer track/[trackingNumber] page
</action>
<verify>Manuel : POST /parcels Yaoundé→Douala 2kg → tracking CM-* + timeline registered ; public /track/CM-* sans auth montre sanitized, sans téléphones complets</verify>

### Task 2 — Pages Events (catalogue + billetterie)
**files:** `apps/web/lib/api/events.ts`, `apps/web/app/events/page.tsx`, `apps/web/app/events/[id]/page.tsx`
**read_first:** `apps/web/app/rentals/page.tsx` (catalogue), `apps/web/app/rentals/[id]/page.tsx` (booking multi-villes), `components/tickets/TicketDetail.tsx`
<acceptance_criteria>
- `lib/api/events.ts` `fetchEvents(params) GET /api/v1/events?search&city&eventType&dateFrom&dateTo&q&page/limit`, `fetchEvent(id)`, `createEventBooking(token,{eventId,ticketCategoryId,quantity}) Idempotency-Key`, `fetchMyEventBookings`, `createEventBookingPayment`
- `app/events/page.tsx` client : `useQuery ["events",params]` filters search Input + city Select (Yaoundé/Douala) + eventType Select (concert/sport/conference/festival/theatre/other) + q + pagination. `EventCard` posterUrl (img `object-cover rounded-xl`), name, venue city, startDate, eventType badge, prix min via `Math.min(...ticketCategories.map(c=>c.price))`, `quantity-sold` urgency `Plus que X places` si `available<10`. Pagination.
- `app/events/[id]/page.tsx` client `useQuery fetchEvent(id)` affiche `posterUrl` hero, `venue/city/startDate/endDate` description, `TicketCategory` cards (name, description, price, available = quantity-sold, status on_sale/limited/sold_out) avec `Stepper qty 1..min(10,available)` + total `price*qty` + `POST` → `router.push(/dashboard?tab=billets)` + QR `TicketDetail` si `ticketNumber/qrCode` retourné. `Sold_out` card grisée + badge.
</acceptance_criteria>
<action>
1. Créer lib/api/events.ts
2. Remplacer ComingSoon par catalogue + fiche billetterie
3. Réutiliser Stepper + PaginationControls
</action>
<verify>Manuel : /events filtre concert → liste ; /events/:id choisit catégorie VIP qty2 → 201 ; last ticket concurrent 1/1</verify>

### Task 3 — Dashboard Global + Admin Parcels/Events
**files:** `apps/web/app/dashboard/page.tsx`, `apps/web/components/dashboard/Dashboard.tsx`, `apps/web/components/admin/AdminShell.tsx`, `apps/web/components/admin/AdminParcels.tsx`, `apps/web/components/admin/AdminEvents.tsx`
**read_first:** `Dashboard.tsx` (3 Tabs après 06-03), `AdminShell.tsx:19` (NAV Hôtels/Véhicules), `AdminHotels.tsx` (table+export)
<acceptance_criteria>
- `Dashboard.tsx` étendre Tabs `Voyages à venir|Hôtels|Véhicules|Colis|Billets` (5 Tabs, `VISIBLE_LIMIT 3` chaque). `Promise.all` côté client `getDashboard + /hotels/bookings/me + /rentals/bookings/me + /parcels + /events/bookings/me` via `apiFetch` avec token. Chaque tab `TabContent` avec `HotelBookingCard/RentalBookingCard/ParcelCard (trackingNumber/status/shippingCost/currentLocation) / EventBookingCard (event name, ticketNumber, qrCode, quantity, total)`. Empty `EmptyState Aucun colis + Envoyer un colis → /parcels`, `Aucune billetterie + Découvrir événements → /events`. `?tab=colis|billets` via `useSearchParams`.
- `AdminShell.tsx` `NAV` ajouter `Colis` + `Événements` (`Parcels`, `Events`), `renderSection` switch ajoute `<AdminParcels|AdminEvents />`
- `AdminParcels.tsx` : `useQuery ["admin-parcels",page,limit,q,recipientCity,status]` `GET /api/v1/admin/parcels?q&recipientCity&status&dateFrom&dateTo&page/limit` + second query `GET /admin/parcels` ? sinon `GET /api/v1/parcels` admin filtré ; table trackingNumber sender→recipient status currentLocation shippingCost + actions `Avancer statut` `PATCH /admin/parcels/:id/status` FSM + `ExportButton` `GET /admin/parcels/export?dateFrom&dateTo&format=csv`.
- `AdminEvents.tsx` : `useQuery ["admin-events",...]` `GET /api/v1/admin/events?q&city&eventType&status&page/limit` + `GET /admin/event-bookings` ; tables events (poster, name, city, startDate, status, sold/quantity) + actions `Publier` `PUT /admin/events/:id` partnerStatus + `POST /admin/events` (admin publie seul conformément décision), categories `quantity/sold` ; exports `GET /admin/events/export` + `GET /admin/event-bookings/export`.
- Backend `admin/routes.ts` déjà partiellement 06-03 (hotels/rentals), ajouter `GET /admin/parcels|events` + `GET /admin/parcels|events/export` + `PATCH /admin/parcels/:id/status` + `GET/PUT /admin/events` + `GET /admin/event-bookings` si absents (via `admin/service.ts` listParcels/listEvents — créer si manquant).
</acceptance_criteria>
<action>
1. Étendre Dashboard 5 Tabs + fetch 2 sources supplémentaires
2. Créer AdminParcels/Events components + API calls + links
3. Ajouter endpoints admin manquants (parcels/events) + liaison AdminShell
</action>
<verify>Dashboard 5 onglets → chaque >3 montre Voir tous ; Admin 2 nouveaux onglets exports csv ; typecheck 0</verify>

### Task 4 — Verification
**files:** all
<acceptance_criteria>
- `pnpm -r typecheck` 0
- `vitest` parcels FSM + events sold concurrent déjà vert côté API (07-01/07-02), web no prisma.
- Manuel `/parcels` POST → track timeline 6 états ; `/events` → fiche → booking → /dashboard?tab=billets QR ; Admin Colis FSM + Admin Events publish
</acceptance_criteria>

## Risks
- Public track sanitized déjà via `parcels/service sanitizedShape` — s'assurer web n'expose pas `userId`
- Admin events `POST /admin/events` admin seul publie — pas d'organizer flow à implémenter (décision validée)
- Dashboard 5 queries parallèles + 3 existantes = 8 : gérer `isLoading` agrégé, `Promise.allSettled` best-effort

## Done When
- 2 parcours web + 1 suivi public + 1 dashboard global 5 tabs + 2 admin onglets + exports, transport hero preserved, typecheck/test verts
