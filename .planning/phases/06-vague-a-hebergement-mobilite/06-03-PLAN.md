# Phase 06 Plan 03: Web + Partner + Admin — Hôtels & Véhicules

## Frontmatter
```yaml
---
phase: 06
plan: 03
wave: 2
depends_on: [06-01, 06-02]
theme: transport hero 2x + 2 cartes secondaires
animation: framer-motion hoverLift, gsap reveal (compositor only)
files_modified:
  - apps/web/app/page.tsx # homepage hero 2x grille
  - apps/web/components/landing/SiteNav.tsx # + Hôtels/Véhicules
  - apps/web/app/hotels/page.tsx
  - apps/web/app/hotels/[id]/page.tsx # new
  - apps/web/app/rentals/page.tsx
  - apps/web/app/rentals/[id]/page.tsx # new
  - apps/web/app/dashboard/page.tsx
  - apps/web/components/dashboard/Dashboard.tsx
  - apps/web/components/admin/AdminShell.tsx
  - apps/web/components/admin/AdminHotels.tsx # new
  - apps/web/components/admin/AdminRentals.tsx # new
  - apps/web/app/partner/hotels/page.tsx # new
  - apps/web/app/partner/rentals/page.tsx # new
  - apps/web/lib/api/hotels.ts # new
  - apps/web/lib/api/rentals.ts # new
files_created:
  - apps/web/app/hotels/[id]/page.tsx
  - apps/web/app/rentals/[id]/page.tsx
  - apps/web/lib/api/hotels.ts
  - apps/web/lib/api/rentals.ts
  - apps/web/components/admin/AdminHotels.tsx
  - apps/web/components/admin/AdminRentals.tsx
  - apps/web/app/partner/hotels/page.tsx
  - apps/web/app/partner/rentals/page.tsx
autonomous: true
requirements:
  - HOTEL-05/RENTAL-05 (web parcours complet)
  - NAV-01 (SiteNav 7 entrées), HOME-01 (hero dominant), DASH-01 (compte unique)
  - ADMIN-04 (admin hôtels/véhicules + exports), TRANS-04 (partner hotels/rentals presigned)
must_haves:
  truths:
    - Homepage `app/page.tsx:69` grille 3 cols : transport `col-span-2 row-span-2` primaire + 5 tuiles secondaires (Hôtels, Véhicules, Colis, Événements, Intraurbain) avec CTA Réserver bus dominant, sous-titre "Le transport interurbain est notre service principal"
    - SiteNav `SiteNav.tsx:10` 7 entrées CdCF §10 : Accueil/Transport interurbain/Hôtels & apparts/Location véhicules/Transport colis/Billetterie événements/Mes réservations/Compte
    - /hotels recherche (ville, dates, pax, prix, q) + filtres + pagination + fiche (photos, équipements, conditions, prix) + panier nuits + paiement + confirmation
    - /rentals catalogue (ville, catégorie, hasDriver, prix, q) + fiche (make/model, capacité, transmission, prix/durée, photos) + dates + pickup/dropoff + hasDriver + conditions + paiement + confirmation
    - Dashboard `app/dashboard` onglets Voyages à venir | Hôtels | Véhicules (agrégé via GET /me/dashboard ou 3 queries)
    - Partner `app/partner/hotels` CRUD hôtel/chambres (presignPut `transporters/${id}/hotels` via @camermove/media) + dispo/qty/prix ; `app/partner/rentals` CRUD véhicule + photos presigned
    - Admin `AdminShell.tsx:19` onglets + Hôtels, Véhicules + listes + exports dateFrom/dateTo csv + gestion partnerStatus
  prohibitions:
    - statement: No transport dilution — hero must be 2x secondary cards visually
      status: active
      verification: screenshot homepage, transport card area >= 2 * hotel card area
    - statement: No direct DB from web — only REST /api/v1
      status: active
      verification: rg "prisma\." apps/web --type ts --type tsx must be empty except dashboard RSC fallback already guarded
---
```

## Context
Web `app/hotels/page.tsx:1` et `rentals/page.tsx:1` sont `ComingSoon` `components/coming-soon.tsx:1`. Dashboard `Dashboard.tsx:49` n'affiche que `Booking` interurbains. `SiteNav.tsx:10` n'a que 5 liens sans Hôtels/Véhicules. `AdminShell.tsx:19` n'a que transport. `app/page.tsx:15` 100% transport. Pas de `partner/hotels|rentals`.

## Tasks

### Task 1 — Homepage & Nav Héros Dominant
**files:** `apps/web/app/page.tsx`, `apps/web/components/landing/SiteNav.tsx`
**read_first:** `app/page.tsx:15` (prisma trips), `SiteNav.tsx:10` (NAV_LINKS), `components/landing/Hero.tsx:51`
<acceptance_criteria>
- `page.tsx` : `Promise.all` existant `minPrice/trips/agencies` + ajout `hotelsCount = prisma.hotel.count({where:{status:"active"}})` et `rentalsCount = prisma.rentalVehicle.count({where:{status:"available"}})` best-effort try/catch ; grille `grid-cols-1 md:grid-cols-3 gap-4` : transport `col-span-2 row-span-2 bg-primary text-primary-foreground p-6 rounded-2xl` avec `Hero` CTA `Réserver un bus`, 5 cartes secondaires `bg-card border` avec icône `lucide-react: Bed, Car, Package, Ticket, Bus` + compteur `X hôtels / Y véhicules`
- `SiteNav.tsx` : `NAV_LINKS` étendu `[{href:"/",label:"Accueil",isLink:true},{href:"/results",label:"Transport interurbain",isLink:true},{href:"/hotels",label:"Hôtels & apparts",isLink:true},{href:"/rentals",label:"Location véhicules",isLink:true},{href:"/parcels",label:"Transport colis",isLink:true},{href:"/events",label:"Billetterie",isLink:true},{href:"/dashboard",label:"Mes réservations",isLink:true}]` + Compte dropdown si auth, mobile overlay `staggerLinks` inchangé
- Footer `SiteFooter` note "Le transport interurbain est notre service principal"
</acceptance_criteria>
<action>
1. Modifier page.tsx grille + counts
2. Étendre SiteNav NAV_LINKS + mobile
3. Vérifier responsive `col-span-2` devient `col-span-1` sur mobile
</action>
<verify>Visuel homepage transport card 2x ; nav 7 entrées desktop+mobile ; typecheck</verify>

### Task 2 — Pages Hotels & Rentals (parcours complet)
**files:** `apps/web/lib/api/hotels.ts`, `apps/web/lib/api/rentals.ts`, `apps/web/app/hotels/page.tsx`, `apps/web/app/hotels/[id]/page.tsx`, `apps/web/app/rentals/page.tsx`, `apps/web/app/rentals/[id]/page.tsx`
**read_first:** `apps/web/app/results/page.tsx:1` (FilterBar+Pagination), `apps/web/lib/api/search.ts` (fetchSearch), `apps/web/components/search/trip-card.tsx`
<acceptance_criteria>
- `lib/api/hotels.ts` `fetchHotels(params) GET /api/v1/hotels?city&checkIn&checkOut&guests&minPrice&maxPrice&q&page&limit`, `fetchHotel(id)`, `createHotelBooking(body) POST /api/v1/hotels/bookings` avec `Idempotency-Key: crypto.randomUUID()`, `fetchMyHotelBookings()`
- `lib/api/rentals.ts` `fetchRentals(params)`, `fetchRental(id)`, `createRentalBooking(body)` idem
- `app/hotels/page.tsx` client : `useState` filters `city/checkIn/checkOut/guests/minPrice/maxPrice/q/page`, `useQuery ["hotels",filters]` debounced 300ms, `FilterBar` (Input city, dates, pax stepper `Stepper` existant `components/ui/stepper.tsx:1`, prix slider, q), `HotelCard` (photo, name, city, starRating, amenities badge, pricePerNight), `PaginationControls`, empty `Bus` → `Bed` icon, lien `→ /hotels/[id]`
- `app/hotels/[id]/page.tsx` client `useQuery fetchHotel(id)` affiche `rooms[]` (name, capacity, bedType, price, quantity, amenities), sélecteur dates+guests, calcul `nights`, `total = price*nights`, `Passenger` names `String[]` inputs, `Recap` + `POST` → `router.push(/dashboard?tab=hotels)` + toast
- `app/rentals/page.tsx` similaire : `pickupCity, category Select, hasDriver Switch, startDate/endDate, q`, `RentalCard` (make model, category, capacity, hasDriver badge, pricePerUnit/durationUnit), pagination
- `app/rentals/[id]/page.tsx` : dates `start/end` inputs, `pickupCity/dropoffCity` (dropoff défaut = pickup), `driverName/Phone` si `hasDriver`, calcul `duration` via véhicule `durationUnit`, recap + POST
- Tous `Zod` côté client via `react-hook-form` si déjà utilisé, sinon `useState` + `fetch` + `sonner` toast
</acceptance_criteria>
<action>
1. Créer lib/api clients
2. Remplacer ComingSoon par recherche+fiche+booking pour 2 services
3. Réutiliser `Stepper`, `PaginationControls`, `AgencyChips` pattern
</action>
<verify>Manuel : /hotels filtre Yaoundé → liste ; /hotels/:id choisit chambre 2 nuits → 201 ; /rentals overlap même véhicule → 409</verify>

### Task 3 — Dashboard Compte Unique
**files:** `apps/web/app/dashboard/page.tsx`, `apps/web/components/dashboard/Dashboard.tsx`
**read_first:** `Dashboard.tsx:1` (VISIBLE_LIMIT 3), `app/dashboard/page.tsx:16` (loadDashboard)
<acceptance_criteria>
- `Dashboard.tsx` props `initialData` étendu `+ hotelBookings, rentalBookings` (fetch `Promise.all` `getDashboard` + `fetchMyHotelBookings` + `fetchMyRentals`) ; `Tabs` `Voyages à venir | Hôtels | Véhicules` (shadcn `Tabs` `components/ui/tabs.tsx`) ; chaque tab `VISIBLE_LIMIT 3` + `Voir tous → /dashboard?tab=hotels|rentals` ; empty `EmptyState "Aucune réservation hôtel"` + CTA `Découvrir Hôtels → /hotels`
- `SectionCards` inchangé, mais `ChartAreaInteractive` peut rester transport seul
- RSC `dashboard/page.tsx` `loadDashboard` étendu `headers cookie` → `GET /api/v1/me/dashboard` agrégé ou 3 fetch séparés avec `x-cm-user-token`
</acceptance_criteria>
<action>
1. Étendre Dashboard fetch + Tabs
2. Ajouter HotelBookingCard / RentalBookingCard (réutiliser `UpcomingTripCard` style)
3. Gérer `tab` query param `useSearchParams`
</action>
<verify>/dashboard affiche 3 onglets, chaque >3 montre Voir tous ; empty states corrects</verify>

### Task 4 — Partner Hotels & Rentals (presigned)
**files:** `apps/web/app/partner/hotels/page.tsx`, `apps/web/app/partner/rentals/page.tsx`, `apps/web/components/partner/*`
**read_first:** `apps/web/app/transporter/apply/page.tsx` (ApplyWizard), `apps/web/components/transporter/Trips.tsx` (CRUD), `packages/media/src/storage.ts:26` (presignPut)
<acceptance_criteria>
- `app/partner/hotels/page.tsx` RSC guard `transporter_staff` (réutiliser `transporter/dashboard` guard `cookies/x-cm-user-token`) ; client `HotelsPartnerClient` : `GET /api/v1/hotels?owner=me` (ou `GET /partner/hotels` si endpoint dédié, sinon `GET /hotels?ownerId` filtré serveur) ; tableau `hotels` + `rooms` expand, CRUD `POST /partner/hotels` avec `POST /partner/hotels/presign` → `put uploadUrl` → `photos[]` objectKeys ; `PUT /partner/hotels/:id` + `POST /partner/hotels/:id/rooms`
- `app/partner/rentals/page.tsx` similaire : liste `rentalVehicles` owner, CRUD `make/model/category/capacity/pricePerUnit/durationUnit/pickupCity/photos` via presigned `rentals/${id}/photos`
- Tous `Zod` + `AuditLog` côté API déjà via `partner/*` si manquant ajouter `POST /partner/hotels|rentals/presign` dans `apps/api/src/partner/*` ou `hotels/routes.ts` + `rentals/routes.ts` (`POST /hotels/presign` `requireAuth transporter_staff`)
</acceptance_criteria>
<action>
1. Créer 2 pages partner RSC + 2 clients CRUD
2. Ajouter 2 endpoints presign `POST /hotels/presign` et `POST /rentals/presign` si absents
3. Gérer `partnerStatus pending→approved` affichage `Badge`
</action>
<verify>Partner peut créer hôtel + 2 chambres + upload 2 photos presigned ; véhicule SUV créé</verify>

### Task 5 — Admin Hôtels & Véhicules
**files:** `apps/web/components/admin/AdminShell.tsx`, `apps/web/components/admin/AdminHotels.tsx`, `apps/web/components/admin/AdminRentals.tsx`
**read_first:** `AdminShell.tsx:19` (NAV), `AdminHotels.tsx` inexistant, `AdminTrips.tsx` (table + export)
<acceptance_criteria>
- `AdminShell.tsx` `NAV` ajouter `Hôtels` + `Véhicules` (ou `Hébergement` + `Mobilité`), `renderSection` switch ajoute `<AdminHotels|AdminRentals />`, accès `admin|super_admin` déjà via `app.requireAuth()` + role check
- `AdminHotels.tsx` : `useQuery ["admin-hotels",page,limit,q,city,status]` `GET /api/v1/admin/hotels` (nécessite nouveau endpoint `admin/routes.ts` `GET /admin/hotels` déjà partiel, sinon créer `admin/hotels.ts` reuse `admin/service.ts listHotels` — créer si manquant), table `hotel + rooms count + city + status + partnerStatus`, actions `approve/reject` (`PUT /admin/hotels/:id`), `ExportButton` `dateFrom/dateTo csv`
- `AdminRentals.tsx` : `GET /api/v1/admin/rentals` `listVehicles` admin, table `make model category pickupCity status partnerStatus`, actions, export
- `AdminBookings` étendu si besoin `admin/hotel-bookings` et `admin/rental-bookings` (`listHotelBookings` déjà `admin/service.ts` ? sinon ajouter `listHotelBookings/rentalBookings`)
</acceptance_criteria>
<action>
1. Créer AdminHotels/Rentals components + API calls
2. Ajouter endpoints `GET /admin/hotels|rentals` + `GET /admin/hotels|rentals/export` si absents
3. Étendre AdminShell NAV + render
</action>
<verify>Admin voit 2 nouveaux onglets, exports csv téléchargés, partnerStatus modifiable</verify>

### Task 6 — Verification
**files:** all
<acceptance_criteria>
- `pnpm -r typecheck` 0
- `pnpm --filter @camermove/api exec vitest run` hotels/rentals overlap
- Manuel `/hotels` + `/rentals` parcours → `/dashboard` tabs ; `SiteNav` 7 entrées ; homepage hero 2x
</acceptance_criteria>

## Risks
- Photos presigned CORS `MINIO_API_CORS_ALLOW_ORIGIN *` déjà `docker-compose.yml:27`
- Partner hotels `ownerId` nullable — filtrer `where ownerId = user.id` côté API, ne pas exposer tous hôtels en partner
- `GET /admin/hotels` manquant → créer `admin/service.ts listHotelsAdmin` si besoin

## Done When
- 2 parcours web + 2 partner CRUD presigned + 2 admin onglets + homepage/nav/dashboard héroïsés, typecheck/test verts
