# Phase 06 Plan 01: API Hotels — Atomique, Cache, Paiement Unifié

## Frontmatter
```yaml
---
phase: 06
plan: 01
wave: 1
depends_on: [05-interurban-marketplace]
theme: transport hero preserved
animation: none
files_modified:
  - packages/db/prisma/schema.prisma # HotelBooking checkInDate/checkOutDate/guestCount already correct, no migration needed beyond 20260903000000
  - packages/db/prisma/migrations/20260903000000_multiservice_payment_fix/migration.sql
  - apps/api/src/hotels/routes.ts
  - apps/api/src/hotels/schema.ts
  - apps/api/src/hotels/service.ts # new
  - apps/api/src/hotels/repository.ts # new
  - apps/api/src/lib/cache.ts
  - apps/api/src/lib/query.ts
  - packages/shared/src/money.ts # hotel nights calc if needed
files_created:
  - apps/api/src/hotels/service.ts
  - apps/api/src/hotels/repository.ts
autonomous: true
requirements:
  - HOTEL-01..04 (recherche, dispo, réservation, paiement, confirmation)
  - AGENTS §1 ACID, §2 metadata, §3 no dead code, §4 modular, §6 exportable
must_haves:
  truths:
    - GET /hotels?city&checkIn&checkOut&guests&minPrice&maxPrice&q&page/limit&orderBy recherche avec cache 60s et invalidation on write, paginé
    - GET /hotels/:id inclut rooms[] + photos presigned via media
    - POST /hotels/bookings est atomique FOR UPDATE : check overlap count < quantity, sinon 409; calcule nights*pricePerNight, totalAmount, holdExpiresAt depuis AppSettings
    - POST /hotels/bookings est idempotent Idempotency-Key 24h, log req.meta, AuditLog, Kafka hotel.booking.created
    - GET /hotels/bookings/me et :id et /export (dateFrom/dateTo csv) respectent RBAC et SEARCH_MAX_LIMIT
    - POST /hotels/bookings/:id/pay réutilise payments/service createPayment polymorphe (bookingId nullable)
  prohibitions:
    - statement: No string field mismatch (checkIn vs checkInDate) — prisma validate must pass
      status: active
      verification: pnpm --filter @camermove/db exec prisma validate && pnpm -r typecheck
    - statement: No race on last room — concurrent bookings for same room/dates must serialize
      status: active
      verification: vitest hotels concurrent overlap (FOR UPDATE) — one 201, one 409
---
```

## Context
Hôtels est stub cassé `apps/api/src/hotels/routes.ts:6` (checkIn/guests vs schema `checkInDate/guestCount:399`, `guestNames String[]` vs `[{fullName}]`, `partner` include inexistant, pas d'ACID/cache/idempotency/Audit/Kafka/export). `schema.ts:3` dead. Pricing `pricePerNight * nights` ok mais pas de dispo calendrier `quantity:390`. Besoin de Vague A atomique avant Rentals.

## Tasks

### Task 1 — Repository + Service ACID
**files:** `apps/api/src/hotels/repository.ts`, `apps/api/src/hotels/service.ts`
**read_first:** `apps/api/src/bookings/service.ts:66` (atomicHoldSeats pattern), `packages/db/src/repositories/seat.repository.ts:1`, `admin/service.ts:242` (audit pattern)
<acceptance_criteria>
- `repository.ts` expose `findHotels(where,skip,take)` et `countHotels(where)` avec indexes `@@index([city,status]):374`
- `service.ts` `createHotelBooking({hotelId,roomTypeId,userId,checkInDate,checkOutDate,guestCount,guestNames,specialRequests})` fait `$transaction` : `SELECT ... FOR UPDATE` sur `HotelRoom` + `SELECT COUNT overlapping HotelBooking where status in [pending_payment,confirmed] AND checkInDate < newCheckOut AND checkOutDate > newCheckIn` ; si `count >= quantity` throw `ConflictError`; sinon crée `HotelBooking` avec `checkInDate/checkOutDate/guestCount/String[]` correct, `totalAmount = pricePerNight * ceil((out-in)/86400)`, `holdExpiresAt` via `AppSettings.holdExpiryMinutes` (cache 30s)
- `service.ts` publie `hotel.booking.created` via `@camermove/events` best-effort + `prisma.auditLog.create` + `invalidateCache("hotels*")`
- `getCached/setCached` 60s sur search, `invalidateCache` on create
</acceptance_criteria>
<action>
1. Créer repository avec where builders (city contains, price range via rooms.some, status active)
2. Créer service ACID + nights calc + Kafka + audit + cache invalidation
3. Exporter `HotelSearchQuery` commun (city, checkIn, checkOut, guests, min/maxPrice, q, page/perPage 50, limit/offset, orderBy, groupBy)
</action>
<verify>`prisma validate` + `typecheck` + unit concurrent test 1 livre / 1 échec sur quantity=1</verify>

### Task 2 — Routes Zod + RBAC + Metadata + Idempotency
**files:** `apps/api/src/hotels/routes.ts`, `apps/api/src/hotels/schema.ts`
**read_first:** `apps/api/src/search/routes.ts:12` (meta log), `apps/api/src/bookings/routes.ts:13` (requireAuth + meta), `apps/api/src/plugins/idempotency.ts:11`
<acceptance_criteria>
- `schema.ts` unique source (supprimer duplication routes.ts) : `CreateHotelBookingBody {hotelId cuid, roomTypeId cuid, checkIn YYYY-MM-DD, checkOut YYYY-MM-DD, guests 1..10, guestNames String[] default[], specialRequests 500}` + `HotelSearchQuery {city?,checkIn?,checkOut?,guests 1..20?,minPrice?,maxPrice?,q?,page/perPage 1..50, limit/offset, orderBy?,groupBy?}`
- `routes.ts` : `GET /hotels` parse `HotelSearchQuery`, `req.log.info({...meta,city,filters}, "hotels.search")`, `getCached` early + `setCached 60` ; `GET /hotels/:id` incl `rooms:true` (pas `partner`) ; `POST /hotels/bookings` `preHandler requireAuth()` + `Idempotency-Key` via global plugin, `req.log.info({...meta,hotelId,roomTypeId,guestCount,userId}, "hotels.booking.create")` + `AuditLog` + Kafka ; `GET /hotels/bookings/me?dateFrom&dateTo&q&page/limit` + `GET /hotels/bookings/:id` owner/admin ; `GET /hotels/bookings/export?dateFrom&dateTo&format` via `parseExportQuery/sendExport` avec `SEARCH_MAX_LIMIT` + `Content-Disposition`
- Tous `Zod` sur chaque endpoint, `AppError` pour 404/409/400, `guestNames` `String[]` pas objets
</acceptance_criteria>
<action>
1. Réécrire schema.ts canonique, supprimer duplication routes.ts
2. Réécrire routes.ts avec service/repository, cache, meta, idempotency, export, RBAC
3. Ajouter `POST /hotels/bookings/:id/pay` délégant à `payments/service.ts createPayment` avec `bookingId=null, hotelBookingId=id` (polymorphe déjà nullable `schema.prisma:276`)
</action>
<verify>`curl GET /hotels?city=Yaoundé | jq .items[0].rooms` + `POST /hotels/bookings` replay Idempotency-Key même body → même 201 ; typecheck 0</verify>

### Task 3 — Verification
**files:** all
<acceptance_criteria>
- `pnpm --filter @camermove/db exec prisma validate` ✅
- `pnpm -r typecheck` 0 (enlever `// @ts-nocheck` hotels après correction)
- `pnpm --filter @camermove/api exec vitest run` — new `hotels/service.test.ts` overlap (FOR UPDATE) : 2 concurrents quantity=1 → 1 succès 1 409
- `rg "checkIn|guests" apps/api/src/hotels --type ts` plus de mismatch
</acceptance_criteria>

## Risks
- Overlap check oublie `checkOut == newCheckIn` (non-overlap) — spécifier `lt/gt` strict, pas `lte/gte`
- Nights off-by-one timezone — utiliser `T00:00:00.000Z` comme `search/service.ts:7`

## Done When
- 3 routes + 2 exports + pay endpoint verts, cache 60s, ACID concurrent, idempotent, audit+kafka, typecheck/test verts
