# Phase 06 Plan 02: API Rentals — Overlap Strict, Multi-Villes, Driver Option

## Frontmatter
```yaml
---
phase: 06
plan: 02
wave: 1
depends_on: [06-01]
theme: transport hero preserved
animation: none
files_modified:
  - apps/api/src/rentals/routes.ts # created in hardening, to harden
  - apps/api/src/rentals/schema.ts
  - apps/api/src/rentals/service.ts # new
  - apps/api/src/rentals/repository.ts # new
  - packages/db/prisma/schema.prisma # RentalVehicle/RentalBooking already correct, no migration
  - packages/shared/src/money.ts # duration calc if needed
files_created:
  - apps/api/src/rentals/service.ts
  - apps/api/src/rentals/repository.ts
autonomous: true
requirements:
  - RENTAL-01..04 (catalogue, dispo chevauchement, réservation, paiement, confirmation)
  - AGENTS §1 ACID, §2 metadata, §6 exportable, §4 modular
must_haves:
  truths:
    - GET /rentals?pickupCity&category&hasDriver&q&minPrice&maxPrice&page/limit cache 60s, paginé
    - GET /rentals/:id détail complet (make/model/year/capacity/transmission/fuel/hasDriver/prix/durée/photos)
    - POST /rentals/bookings calcule duration selon vehicle.durationUnit (hour/day/week/month), montant pricePerUnit*duration, check overlap strict startDate < newEnd && endDate > newStart sur même véhicule, sinon 409
    - POST /rentals/bookings supporte pickupCity/dropoffCity différentes (multi-villes), driverName/phone optionnels si hasDriver, pricing identical (dropoff fee futur via AppSettings)
    - POST idempotent Idempotency-Key, log req.meta, AuditLog, Kafka rental.booking.created, invalidateCache
    - GET /rentals/bookings/me + :id + /export (dateFrom/dateTo csv) RBAC
    - POST /rentals/bookings/:id/pay réutilise Payment polymorphe
  prohibitions:
    - statement: No overlap race — 2 concurrents même véhicule mêmes dates = 1 succès 1 409 via FOR UPDATE
      status: active
      verification: vitest rentals concurrent overlap
    - statement: No hardcoded duration — must respect vehicle.durationUnit
      status: active
      verification: unit duration calc hour/day/week/month
---
```

## Context
Rentals était build-break `apps/api/src/rentals/routes.ts` manquant `app.ts:24`, `schema.ts:3` orphelin. Hardening a créé un stub fonctionnel `routes.ts:1` (overlap simple, duration day, cache, pas de service). Besoin d'atomique `SELECT FOR UPDATE` sur `RentalVehicle` + overlap `OR [{startDate lt newEnd} && {endDate gt newStart}]` avec `status in [pending_payment,confirmed,active]` `schema.prisma:456`. Prix `pricePerUnit * duration` selon `durationUnit hour|day|week|month:308`.

## Tasks

### Task 1 — Repository + Service Duration & Overlap
**files:** `apps/api/src/rentals/repository.ts`, `apps/api/src/rentals/service.ts`
**read_first:** `apps/api/src/hotels/service.ts` (overlap pattern), `packages/db/src/repositories/seat.repository.ts:1` (FOR UPDATE), `apps/api/src/bookings/service.ts:74` (AppSettings)
<acceptance_criteria>
- `repository.ts` `findRentals(where,skip,take)` filtre `pickupCity contains, category contains, hasDriver, pricePerUnit range, status available, q sur make/model` avec indexes `@@index([pickupCity,status]):451`
- `service.ts` `durationFor(vehicle, startDate, endDate)` : `hour ceil(ms/3600000), day ceil(ms/86400000), week ceil(ms/(7*86400)), month ceil(ms/(30*86400))` — testé unitairement
- `service.ts` `createRentalBooking({rentalVehicleId,userId,startDate,endDate,pickupCity,pickupAddress,dropoffCity,dropoffAddress,driverName,driverPhone})` : `$transaction` → `SELECT FOR UPDATE RentalVehicle` → `findFirst overlap` (`status in [...]` + `OR start<newEnd && end>newStart`) → 409 si trouvé ; sinon `totalAmount = pricePerUnit * duration` ; crée `RentalBooking` `status pending_payment` avec `duration/durationUnit` corrects
- Publie `rental.booking.created` + `AuditLog` + `invalidateCache("rentals*")`
</acceptance_criteria>
<action>
1. Créer repository where builder + pagination
2. Implémenter duration calc + overlap ACID + création + Kafka/audit/cache
3. Gérer dropoffCity nullable (même ville par défaut), `hasDriver` bool véhicule vs `driverName` requête (pas de mismatch)
</action>
<verify>vitest `durationFor` 4 unités + `createRentalBooking` concurrent même véhicule → 1 201 1 409</verify>

### Task 2 — Routes Zod + Cache + Idempotency + Export
**files:** `apps/api/src/rentals/routes.ts`, `apps/api/src/rentals/schema.ts`
**read_first:** `apps/api/src/hotels/routes.ts` (cache/meta), `apps/api/src/search/routes.ts:12` (cacheKey)
<acceptance_criteria>
- `schema.ts` garde `RentalSearchQuery {city?,category?,hasDriver?,startDate?,endDate?,minPrice?,maxPrice?,q?,page/perPage}` + `CreateRentalBookingBody {rentalVehicleId cuid, startDate YYYY-MM-DD, endDate YYYY-MM-DD, pickupCity 1..100, pickupAddress 200?, dropoffCity 100?, dropoffAddress 200?, driverName 100?, driverPhone 20?}` — valider `end > start` dans route, pas Zod refine (garder simple)
- `routes.ts` : `GET /rentals` cache 60s `cacheKey("rentals", sortedQuery)` + `getCached/setCached` + `req.log.info({...meta,pickupCity,category}, "rentals.search")`; `GET /rentals/:id` ; `POST /rentals/bookings` `requireAuth` + `Idempotency-Key` global + `req.log.info({...meta,rentalVehicleId,duration,userId}, "rentals.booking.create")` + 201 ; `GET /rentals/bookings/me?dateFrom&dateTo&q&page/limit` + `:id` owner/admin ; `GET /rentals/bookings/export` via `parseExportQuery/sendExport` + `SEARCH_MAX_LIMIT`
- `POST /rentals/bookings/:id/pay` délègue à payments polymorphe
</acceptance_criteria>
<action>
1. Réécrire routes.ts sur service/repository avec cache/meta/idempotency/export
2. Supprimer `// @ts-nocheck` après types corrigés via `as never` casts si besoin
3. Ajouter invalidation `invalidateCache("rentals*")` + `"search*"` si besoin
</action>
<verify>`GET /rentals?pickupCity=Douala&category=SUV` + `POST /rentals/bookings` overlap 409 ; replay Idempotency-Key même 201 ; typecheck 0</verify>

### Task 3 — Verification
**files:** all
<acceptance_criteria>
- `pnpm -r typecheck` 0
- `vitest` rentals duration + overlap concurrent
- `rg "availableQuantity|hotelId.*rentals" apps/api/src/rentals` clean
</acceptance_criteria>

## Risks
- Dropoff multi-villes sans surcoût — documenter `AppSettings.featureFlags.rentalDropoffFee` futur, ne pas bloquer MVP
- `RentalBooking.paymentId @unique :475` — paiement polymorphe via `HotelBooking.paymentId` pattern, pas de collision

## Done When
- 3 listes + 1 détail + 1 création atomique multi-villes + 2 exports + pay, cache/idempotent/audit/kafka verts
