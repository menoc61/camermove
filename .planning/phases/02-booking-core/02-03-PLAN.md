---
phase: 02-booking-core
plan: 03
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [BOOK-02, BOOK-05]
files_modified:
  - apps/worker/src/index.ts
  - apps/api/src/bookings/service.ts
  - apps/api/src/bookings/expire-holds.integration.test.ts
  - packages/db/src/repositories/seat.repository.test.ts
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/*_user_transporter_link/migration.sql
  - apps/api/src/search/trip-status.ts
  - apps/api/src/search/trip-status.test.ts
  - apps/api/src/search/routes.ts
  - apps/api/src/bookings/routes.ts
user_setup: []

must_haves:
  truths:
    - "A pending_payment booking whose holdExpiresAt passes is automatically moved to status=expired and its seats return to available within ~one minute of expiry (BOOK-02 / SC2)"
    - "An authenticated transporter_staff can pause or close their own trip and reopen it; a paused/closed trip is hidden from default search and blocks new bookings"
    - "A transporter_staff CANNOT change the status of a trip belonging to another transporter (403)"
    - "An admin/super_admin can pause/close/reopen ANY trip"
    - "POST /trips/bulk requires authentication; non-admin authenticated callers get 403; unauthenticated callers get 401"
    - "Expired-hold regression test and permanent concurrent last-seat race test exist in the committed repo (not ephemeral probes)"
    - "A booking with an active pending/processing Payment is NEVER auto-expired by the scheduler - late payment success can still confirm it (no paid-but-seatless outcome)"
  prohibitions:
    - "NO production code path may leave expireHolds without at least one non-test caller"
    - "NO unauthenticated route may mutate Trip.status or delete trips"
    - "Client input may never set an arbitrary Trip.status string — only the server-side action whitelist maps to statuses"
    - "Do NOT implement UI surfaces for pause/close (frontend belongs to Phase 5 transporter workspace)"
    - "Do NOT rewrite the tautological unit cases in service.test.ts or wire the empty worker event stubs (both tracked separately: Info-severity anti-patterns, stubs consumed in Phase 4)"
  artifacts:
    - path: "apps/worker/src/index.ts"
      provides: "60s expireHolds scheduling interval + SIGTERM clear"
      contains: "expireHolds"
    - path: "apps/api/src/search/trip-status.ts"
      provides: "TripStatusActionSchema + setTripStatus with ownership enforcement"
      min_lines: 40
    - path: "apps/api/src/search/routes.ts"
      provides: "POST /trips/:id/status (authed) + authed admin-only POST /trips/bulk"
      contains: "requireAuth"
    - path: "packages/db/prisma/schema.prisma"
      provides: "User.transporterId nullable FK to Transporter"
    - path: "apps/api/src/bookings/expire-holds.integration.test.ts"
      provides: "Live-PG regression: expired hold -> status expired + seats returned; future hold untouched"
    - path: "packages/db/src/repositories/seat.repository.test.ts"
      provides: "Permanent concurrent last-seat race case (Promise.allSettled)"
  key_links:
    - from: "apps/worker/src/index.ts"
      to: "apps/api/src/bookings/service.ts"
      via: "setInterval dynamic import calling expireHolds every 60000ms"
      pattern: "expireHolds"
    - from: "apps/api/src/bookings/service.ts"
      to: "apps/api/src/bookings/repository.ts"
      via: "expireHolds queries via findExpiredHolds() instead of inline findMany"
      pattern: "findExpiredHolds"
    - from: "apps/api/src/search/routes.ts"
      to: "apps/api/src/search/trip-status.ts"
      via: "POST /trips/:id/status preHandler requireAuth + setTripStatus call"
      pattern: "setTripStatus"
    - from: "apps/api/src/search/trip-status.ts"
      to: "packages/db/prisma/schema.prisma"
      via: "ownership check: user.transporterId === trip.transportId for transporter_staff"
      pattern: "transporterId"
---

<objective>
Close the two verification gaps blocking Phase 2 (BOOK-02, BOOK-05):

**Gap 1 (BOOK-02 / Roadmap SC2):** `expireHolds()` is implemented (apps/api/src/bookings/service.ts:79) with its supporting index `@@index([status, holdExpiresAt])`, but has ZERO callers — abandoned pre-payment bookings hold seats forever. Schedule it in the worker on a 60-second interval and prove the behavior with a committed live-Postgres regression test. While in the area, commit the verifier's recommended permanent concurrent last-seat race test (SC1 evidence currently exists only as an ephemeral probe).

**Gap 2 (BOOK-05 clause 2 / Roadmap SC3):** No authenticated endpoint mutates Trip.status, and the sole status-mutating primitive `POST /trips/bulk` (apps/api/src/search/routes.ts:17) has NO auth preHandler — anyone can deactivate or DELETE arbitrary trips. Add a transporter-owned/admin pause-close-reopen endpoint with real ownership enforcement, and lock `/trips/bulk` behind admin auth with audit logging per AGENTS.md §2.

Plus AGENTS.md §3 no-dead-code remediation flagged in the same verification report (bookings/routes.ts unused-client fallback block).

Purpose: restore the phase contract — seat availability must self-heal, and offer lifecycle must be authenticated and ownership-scoped.
Output: Working scheduler wiring, new trip-status endpoint + migration, hardened bulk route, three committed test suites.

Out of scope (tracked elsewhere, do NOT touch): empty worker event stubs (Phase 4 consumes them), tautological arithmetic cases in bookings/service.test.ts (Info severity), frontend pause/close UI (Phase 5 transporter space).
</objective>

<execution_context>
@C:/Users/DTA_WorkStation/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/DTA_WorkStation/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-booking-core/02-VERIFICATION.md
@apps/worker/src/index.ts
@apps/api/src/bookings/service.ts
@apps/api/src/bookings/repository.ts
@apps/api/src/search/routes.ts
@apps/api/src/search/advanced.ts
@apps/api/src/auth/plugins.ts
@packages/db/prisma/schema.prisma
@packages/db/src/repositories/seat.repository.test.ts

**Key facts established during planning (trust these, verified 2026-08-25):**
- `requireAuth(role?)` (auth/plugins.ts) does EXACT single-role match — multi-role routes use bare `app.requireAuth()` then branch on `req.user.role` in-handler (see bookings/routes.ts lines 61–66 admin branch for the established pattern).
- Role enum values: `traveler | transporter_staff | admin | super_admin`. There is currently NO User→Transporter link — Task 2 adds one.
- `Trip.transportId` references Transporter (relation "TripTransporter") — this is the ownership field.
- `createBooking` already gates `trip.status !== "active"` → ConflictError; search defaults `status:'active'` — so setting status to a non-active value immediately hides offers and blocks booking. Only the WRITE side is missing.
- Live-PG vitest harness works today via `pnpm --filter @camermove/db test` (docker compose Postgres Up per STATE.md) — mirror its fixture/cleanup style for all new DB-touching tests.
- No smoke script or committed code calls `POST /trips/bulk`, so adding auth breaks nothing in-repo.
- Worker already stores interval handles on `globalThis.__reconcileInterval` and clears them in SIGTERM — follow this exact pattern for the new interval.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schedule expireHolds in worker (60s interval) + live-PG regression tests + permanent concurrent race test [Gap 1, BOOK-02]</name>
  <files>apps/worker/src/index.ts, apps/api/src/bookings/service.ts, apps/api/src/bookings/expire-holds.integration.test.ts, packages/db/src/repositories/seat.repository.test.ts</files>
  <read_first>
    - apps/worker/src/index.ts — existing hourly `reconcileStalePayments` interval + globalThis handle + SIGTERM clearing (lines 29–51): copy this pattern exactly for the new interval
    - apps/api/src/bookings/service.ts — `expireHolds()` (lines 79–93): keep transactional body unchanged, only replace the inline expired-booking query
    - apps/api/src/bookings/repository.ts — `findExpiredHolds()` (lines 7–9): currently orphaned; becomes the query source
    - packages/db/src/repositories/seat.repository.test.ts — fixture/cleanup harness to mirror (transporter→route→trip+seatAvailability seeding, afterAll deleteMany cascade teardown)
    - packages/db/src/repositories/seat.repository.ts — `atomicHoldSeats` signature used by both test files
  </read_first>
  <action>
    1. In `apps/api/src/bookings/service.ts`: import `findExpiredHolds` from `"./repository"` and replace the inline `prisma.booking.findMany({ where: { status: "pending_payment", holdExpiresAt: { lt: new Date() } } })` call inside `expireHolds` with `await findExpiredHolds()`. Change nothing else in the function signature.
    1b. **LOGIC HARDENING (mandatory — prevents money loss):** update BOTH the query in `apps/api/src/bookings/repository.ts` `findExpiredHolds()` AND the per-booking transaction body in `expireHolds` to close two races against Phase 3 payments:
        - **Race A — paid-but-expired hold:** a booking with an active `Payment` (`status` in `"pending"|"processing"`) whose `holdExpiresAt` passes must NOT be auto-expired; if it were, the late success webhook would find `booking.status !== "pending_payment"` and skip confirmation → customer charged, no seats. Add to the `findExpiredHolds` where clause: `payments: { none: { status: { in: ["pending", "processing"] } } }`.
        - **Race B — expiry vs concurrent confirm:** the current tx updates status unconditionally without a lock, so it can overwrite `confirmed` (set moments earlier by the payment worker under `FOR UPDATE`) and double-release seats. Inside the `$transaction`, FIRST lock and re-check: `await tx.$queryRaw`SELECT "id","status","tripId","seatCount" FROM "Booking" WHERE "id"=${b.id} FOR UPDATE`` then re-fetch via `tx.booking.findUnique`; if the row is gone or `status !== "pending_payment"` → `return` (skip, do not count). Only then update to `expired` and release seats (existing guarded release stays).
  2. In `apps/worker/src/index.ts`: add a second `setInterval` with 60_000 ms cadence that dynamic-imports `"../../api/src/bookings/service.js"` and calls `expireHolds()`, logging the returned count when greater than zero (e.g. console.log "expireHolds released N booking(s)") and catching/logging errors without crashing the worker (mirror the existing `.then/.catch` reconciliation chain). Store the timer handle on globalThis as a sibling property next to the existing reconcile handle (same cast pattern), and clear BOTH intervals in the SIGTERM handler. Add a brief comment noting the BullMQ repeatable-job upgrade path, matching the tone of the existing comment above the reconcile interval.
    3. Create `apps/api/src/bookings/expire-holds.integration.test.ts`: vitest file, live Postgres (requires `docker compose up -d` running �?" same precondition as the db-package suite). Fixture in beforeAll: create transporter, route, trip with totalSeats 4 + seatAvailability {seatsAvailable: 4, seatsHeld: 0, seatsBooked: 0}, and a user; call `atomicHoldSeats(tripId, 4)` (available 0 / held 4); insert THREE booking rows directly via `prisma.booking.create` (bypassing createBooking so timestamps are controlled): booking A `pending_payment` seatCount 2 with `holdExpiresAt` in the past (now minus 60s); booking B `pending_payment` seatCount 1 with `holdExpiresAt` in the future (now plus 15 minutes); booking C `pending_payment` seatCount 1 with `holdExpiresAt` ALSO in the past but WITH an attached `Payment` row `{ provider: "notchpay", amount: C.totalAmount, currency: "XAF", status: "processing" }` �?" generate unique references via the exported `generateReference()`. Test: invoke `expireHolds()`; assert the returned count is exactly 1; reload booking A and expect status `expired`; reload booking B and expect it STILL `pending_payment`; reload booking C and expect it STILL `pending_payment` (active payment protects it from auto-expiry �?" Race A regression); reload seatAvailability and expect exactly `seatsAvailable: 2, seatsHeld: 2` (only A's two seats returned; B and C still held) �?" this assertion set proves release, selectivity, AND payment protection. afterAll: deleteMany payments by booking ids, bookings by tripId, then trip, route, transporter, user, and `prisma.$disconnect()`.
    4. Extend `packages/db/src/repositories/seat.repository.test.ts` with a dedicated describe block "concurrent last-seat race" committing the verifier's probe permanently: module-scope variable for a fresh race-fixture trip created INSIDE the test (totalSeats 1, availability 1) so it cannot interfere with the shared sequential fixtures; fire `Promise.allSettled([atomicHoldSeats(raceTripId, 1), atomicHoldSeats(raceTripId, 1)])`; assert exactly one fulfilled and one rejected; assert final seatAvailability reads seatsAvailable 0 / seatsHeld 1. Track the race trip id for deletion in afterAll (extend the existing teardown or add a sibling afterAll hook).
    Reason for direct-row insertion in step 3: `createBooking` computes holdExpiresAt from AppSettings at call time and cannot produce an already-expired hold deterministically.
  </action>
  <verify>
    <automated>docker compose up -d; pnpm --filter @camermove/db test; pnpm --filter @camermove/api test -- expire-holds; node -e "const fs=require('fs');const s=fs.readFileSync('apps/worker/src/index.ts','utf8');if(!/expireHolds/.test(s))process.exit(1);if(!/60000|60 \* 1000/.test(s))process.exit(2)"</automated>
  </verify>
  <acceptance_criteria>
    - Worker source references expireHolds inside a timed interval with error-isolated invocation
    - Both interval handles are cleared on SIGTERM
    - Integration test asserts: expired booking becomes status expired, future booking untouched, seatsAvailable restored by exactly the expired booking's seatCount, seatsHeld decremented accordingly
    - Committed concurrent race test proves exactly-one-winner on a 1-seat trip
    - `pnpm -r typecheck` reports 0 errors
  </acceptance_criteria>
  <done>A booking abandoned before payment self-expires and releases its seats automatically; SC1 concurrency evidence is a permanent committed test (BOOK-02 truth achievable end-to-end)</done>
</task>

<task type="auto">
  <name>Task 2: Authenticated transporter-owned pause/close/reopen endpoint + lock down POST /trips/bulk behind admin auth [Gap 2, BOOK-05]</name>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma/migrations (generated via migrate dev), apps/api/src/search/trip-status.ts, apps/api/src/search/trip-status.test.ts, apps/api/src/search/routes.ts</files>
  <read_first>
    - packages/db/prisma/schema.prisma — User model (line 83, has NO transporter link yet), Transporter model (line 113), Trip model (line 171, ownership field `transportId`)
    - apps/api/src/auth/plugins.ts — requireAuth is exact-role match; bare requireAuth() admits any authenticated role
    - apps/api/src/bookings/routes.ts lines 61–66 — established in-handler admin branching pattern to follow
    - apps/api/src/search/routes.ts — current unauthenticated bulk registration (lines 17–20) and TripParams-free param handling style
    - apps/api/src/bookings/repository.ts / cancellation.ts — NotFoundError/ForbiddenError import sources and AuditLog data shape used across the API
  </read_first>
  <action>
    1. Schema link (prerequisite for ownership enforcement): in `packages/db/prisma/schema.prisma` add to the User model `transporterId String?` plus relation `transporter Transporter? @relation(fields: [transporterId], references: [id], onDelete: SetNull)`; add back-relation `staffUsers User[]` on the Transporter model (verified 2026-08-25: User currently has NO transporter link; the transporterId hits at schema lines 285/317 belong to Notification/PartnerApplication, not User). Generate the migration with `pnpm --filter @camermove/db exec prisma migrate dev --name user_transporter_link`, then `pnpm --filter @camermove/db exec prisma generate`. FALLBACK if migrate dev hangs/prompts on this machine (known Windows/Docker flakiness): run `prisma db push` to sync, then create the migration folder via `prisma migrate diff --from-schema-datamodel prisma/schema.prisma --to-migrations packages/db/prisma/migrations --script > packages/db/prisma/migrations/<ts>_user_transporter_link/migration.sql` and mark it with `prisma migrate resolve --applied user_transporter_link` — CI runs `migrate deploy`, so the recorded migration MUST exist in git either way. Rationale: the gap demands "transporter may only mutate own trips", which is impossible without a user→transporter identity link; a nullable FK is minimal and Phase 5's transporter workspace reuses it. Email-coincidence matching was rejected as non-enforceable ownership.
    2. Create `apps/api/src/search/trip-status.ts`: export Zod `TripStatusActionSchema = z.object({ action: z.enum(["pause", "close", "reopen"]) })` and a server-side constant map pause→"paused", close→"closed", reopen→"active". Export `async function setTripStatus(input: { tripId: string; action: "pause"|"close"|"reopen"; actor: { id: string; role: string } })`: load the trip (throw NotFoundError "Trajet introuvable" when absent); authorization branch — if role is `transporter_staff`: fetch the actor user's transporterId (select only that column); missing link throws ForbiddenError "Accès refusé"; mismatch between that transporterId and `trip.transportId` throws ForbiddenError "Accès refusé"; else if role is neither admin nor super_admin, throw ForbiddenError "Accès refusé". Then update Trip.status via the whitelist map ONLY (clients never supply raw status strings), write a best-effort AuditLog row (actorId, action "trip.status", entityType "Trip", entityId tripId, metadata { from, to, actorId, role }), and return { id, status }. Keep the file under ~80 lines (AGENTS.md §4).
    3. In `apps/api/src/search/routes.ts`: register `POST "/trips/:id/status"` with preHandler `app.requireAuth()`; parse the id param with a cuid Zod schema (mirror BookingParams), parse body with TripStatusActionSchema, log `{ ...meta, entityId, userId, role }` under slug "trip.status" via req.log.info (metadataPlugin guarantees req.meta), call setTripStatus, return its result.
    4. Harden `POST "/trips/bulk"` in the same file: add preHandler `app.requireAuth()`; inside the handler, after BulkActionSchema parsing, reject unless `user.role` is admin or super_admin (ForbiddenError "Accès refusé") — bulk cross-owner operations are an admin primitive; transporters get granular control through the new per-trip endpoint (this is the orchestrator-sanctioned "requireAuth admin" option). Per AGENTS.md §2 endpoint-metadata table, log `{ ...meta, ids, action, actorId, ip }` and persist a best-effort AuditLog row (entityType "Trip", entityId joined ids, metadata { ids, action, affected }).
    5. Create `apps/api/src/search/trip-status.test.ts`: live-PG vitest covering the full authorization matrix. Fixtures: transporters T1 and T2; user STAFF1 (role transporter_staff, transporterId T1), STAFF2 (transporter_staff, transporterId T2), STAFF0 (transporter_staff, no transporterId), ADMIN (role admin), TRAV (role traveler); two trips owned by T1 (seed route + seatAvailability following the seat.repository.test.ts harness). Assertions: STAFF1 pause own trip → response status "paused" and DB row updated; STAFF1 reopen → "active"; STAFF1 close → "closed"; STAFF2 targeting T1's trip rejects with ForbiddenError and leaves status unchanged; STAFF0 rejects; TRAV rejects; ADMIN closes any trip → "closed". afterAll teardown deletes users, trips, routes, transporters, disconnects.
  </action>
  <verify>
    <automated>pnpm --filter @camermove/db exec prisma migrate status; pnpm -r typecheck; pnpm --filter @camermove/api test -- trip-status; node -e "const fs=require('fs');const s=fs.readFileSync('apps/api/src/search/routes.ts','utf8');const i=s.indexOf('/trips/bulk');if(i<0)process.exit(1);if(!/requireAuth/.test(s.slice(Math.max(0,i-200),i+300)))process.exit(2)"</automated>
  </verify>
  <acceptance_criteria>
    - Migration applied; `prisma migrate status` reports up-to-date; User model carries the nullable transporter link
    - transporter_staff can transition OWN trips through paused/closed/active; foreign trips, unlinkable staff, travelers → ForbiddenError
    - Admin transitions any trip; every transition writes an AuditLog row and metadata log line
    - Unauthenticated request to POST /trips/bulk fails at the preHandler (401); authenticated traveler/staff gets 403; admin passes
    - Status values originate exclusively from the server-side action map
    - `pnpm -r typecheck` 0 errors
  </acceptance_criteria>
  <done>SC3 pause/close capability exists behind authentication with enforced transporter ownership, and the previously unauthenticated bulk deactivate/delete primitive is admin-only (BOOK-05 fully satisfied at API level)</done>
</task>

<task type="auto">
  <name>Task 3: Remove dead-code fallback block in bookings bulk-cancel tail (AGENTS.md §3)</name>
  <files>apps/api/src/bookings/routes.ts</files>
  <read_first>
    - apps/api/src/bookings/routes.ts lines 46–82 — the bulk-cancel handler; the offending block sits between the per-booking cancel loop (ends line 70) and the audit-log block (starts line 76)
  </read_first>
  <action>
    Delete the dead block in the traveler branch of the bulk-cancel handler: the redundant dynamic import of the database client whose binding is never used, together with the conditional wrapper whose body merely discards that binding (currently spanning roughly lines 67–75, including its explanatory comment about a fallback owner-filter path that performs no work). The traveler path already delegates each id through the policy-aware cancel loop and returns the fulfilled count — the block contributes nothing. Preserve everything else: the admin branch above it, the per-booking Promise.allSettled loop, the best-effort AuditLog below it, and the legitimate dynamic client imports in the GET detail and export handlers. Rationale: verification flagged this as a Warning-level AGENTS.md §3 violation (no-dead-code) inside the same gap report being remediated.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('apps/api/src/bookings/routes.ts','utf8');if(/\bvoid\s+p\b/.test(s))process.exit(1)"; pnpm --filter @camermove/api test</automated>
  </verify>
  <acceptance_criteria>
    - The no-op fallback block and its unused client binding are gone from the bulk-cancel handler tail
    - GET /bookings/:id and /bookings/export dynamic imports remain functional (api suite still passes)
    - `pnpm -r typecheck` 0 errors
  </acceptance_criteria>
  <done>Bulk-cancel handler contains no unreachable/no-op code; AGENTS.md §3 spot-check clean for the bookings module</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → API | Previously crossed freely into `POST /trips/bulk` (arbitrary trip deactivate/delete) — this plan closes it |
| authenticated transporter_staff → API | New privileged surface (`POST /trips/:id/status`); horizontal privilege escalation (mutating foreign trips) is the core risk |
| worker process → Postgres | New recurring write path (expireHolds) touching Booking + SeatAvailability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02G-01 | Spoofing / Elevation of Privilege | POST /trips/bulk | mitigate | requireAuth preHandler (401 without token) + in-handler admin-only branch (403 otherwise); Task 2 verify includes a structural gate proving requireAuth guards the bulk registration |
| T-02G-02 | Elevation (horizontal, BOLA) | setTripStatus transporter branch | mitigate | Ownership check `user.transporterId === trip.transportId` with ForbiddenError; full denial matrix covered in trip-status.test.ts incl. unlinked-staff and traveler cases |
| T-02G-03 | Repudiation | Trip status transitions + bulk actions | mitigate | Best-effort AuditLog rows + req.log.info with req.meta (actorId, role, ids, ip) per AGENTS.md §2 metadata table |
| T-02G-04 | Denial of Service | expireHolds 60s interval | mitigate | Query rides the existing `@@index([status, holdExpiresAt])`; errors caught per-tick so a bad row cannot kill the worker; hourly payment reconciliation left untouched |
| T-02G-05 | Tampering | Trip.status free-string field | mitigate | Server-side whitelist map (pause/close/reopen → paused/closed/active) is the only writer; request bodies carry an action enum, never a status value |
| T-02G-06 | Information Disclosure | setTripStatus error messages | accept | Uniform "Accès refusé"/NotFound messages reveal no resource existence or ownership detail beyond what search already exposes publicly |
| T-02G-SC | Supply Chain Tampering | package installs | mitigate | None required — this plan introduces ZERO new dependencies (setInterval pattern reused; BullMQ upgrade remains a documented later swap). slopcheck checkpoint not triggered |
</threat_model>

<verification>
Preconditions: docker compose services Up (Postgres/Redis/Kafka) — already running per STATE.md.

1. `pnpm -r typecheck` → 10 projects, 0 errors
2. `pnpm --filter @camermove/db test` → existing 4 cases + new concurrent-race case all pass (proves exactly-one-winner persists as committed evidence)
3. `pnpm --filter @camermove/api test` → existing suites + expire-holds integration + trip-status authorization matrix all pass
4. Caller gate: worker source references expireHolds in a timed interval (structural check embedded in Task 1 verify)
5. Wiring gate: `findExpiredHolds` referenced by bookings/service.ts (orphaned export resolved)
6. Auth gate: requireAuth present in the `/trips/bulk` registration neighborhood (structural check embedded in Task 2 verify)
6b. Payment-protection gate: findExpiredHolds where-clause excludes bookings with pending/processing payments AND integration test asserts booking C stays pending_payment
7. Migration gate: `pnpm --filter @camermove/db exec prisma migrate status` → Database up-to-date
8. Dead-code gate: no-match on the discarded-binding symbol in bookings/routes.ts (Task 3 verify)

Re-run `/gsd-verify-phase 2` afterwards — expected score 8/8 truths (BOOK-02 unblocked, BOOK-05 complete).
</verification>

<success_criteria>
- An expired hold disappears from "held" state and its seats become bookable again without any manual intervention (verifiable by inserting a past-dated booking, waiting one worker tick, observing status=expired + restored availability)
- A transporter_staff JWT pauses/closes/reopens their own trip; the same token gets 403 on a competitor's trip
- A curl without Authorization header against POST /trips/bulk receives 401; with a traveler token receives 403
- All three new/extended test suites green in CI-reproducible commands (< 60s each)
- Zero new dependencies added; threat register dispositions all honored
</success_criteria>

<output>
Create `.planning/phases/02-booking-core/02-03-SUMMARY.md` when done (frontmatter: phase: 02-booking-core, plan: 03, gap_closure: true, requirements-completed: [BOOK-02, BOOK-05])
</output>
