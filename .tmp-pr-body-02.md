# Phase 2: Booking Core

## Summary

**Phase 2: Booking Core**
**Goal:** Travelers can hold seats and complete passenger info without double-booking.
**Status:** Verified ✓ (02-VERIFICATION.md `status: passed`)

Atomic hold via `SELECT FOR UPDATE`, idempotency, metadata, and frontend booking flow. Builds on Phase 1 (already merged to `master` e64ed8c, default → master). This phase adds repository abstraction, `service.test.ts`, and hold-expiry/cancellation polish. Endpoint sweep shows `POST /bookings` 201 + idempotent replay same `CM-` reference.

## Changes

### feat(bookings): repository + metadata log + unit tests (22db229)
- `apps/api/src/bookings/repository.ts` — `findBookingById`, `findExpiredHolds`, `createBookingRecord`
- `apps/api/src/bookings/service.test.ts` — reference format + totalAmount + hold TTL unit tests
- `apps/api/src/bookings/routes.ts` — `req.log.info({ ...req.meta, tripId, seatCount, passengerCount, userId }, "booking.create")` for AGENTS.md metadata

**Key files:**
- `apps/api/src/bookings/repository.ts`, `service.test.ts`, `routes.ts`, `service.ts`, `cancellation.ts`, `schema.ts`
- `apps/web/app/book/[tripId]/page.tsx`, `apps/web/components/booking/passenger-form.tsx`, `recap.tsx`

### docs(02): verify Booking Core passed (a394d02)
- `02-VERIFICATION.md` passed + `STATE.md` progress update

## Requirements Addressed

BOOK-01..05 (Phase 2)
- BOOK-01 atomic hold, no double-booking
- BOOK-02 hold expiry releases
- BOOK-03 totalAmount = price*seatCount
- BOOK-04 unique CM- reference
- BOOK-05 cancel per policy + bulk cancel
- + idempotency `Idempotency-Key` + metadata `ip/os/browser`

## Verification

- [x] `pnpm -r typecheck` — 0 errors
- [x] `pnpm --filter @camermove/api test` — 7 passed (3 files)
- [x] `POST /bookings` with `Idempotency-Key` replay → same reference (manual sweep 2026-08-25)
- [x] `GET /bookings/:id`, `POST /bookings/:id/cancel`, `POST /bookings/bulk/cancel`, `GET /bookings/export` — PASS (Phase 1 sweep + Phase 2 idempotency)
- [x] Seat hold `SELECT FOR UPDATE` — `seat.repository.test.ts` 4 tests (last-seat concurrent)
- [x] Frontend: `useBookingStore` + passenger-form validation + recap hold countdown

## Key Decisions

Inherited from Phase 1 (Prisma 6, Kafka+BullMQ, etc.) — no new decisions; Phase 2 reuses existing seat primitives and Redis TTL.

## TDD Audit

| Test commit | Impl commit | gate_status |
|---|---|---|
| — | `22db229` feat(bookings): repository + metadata log + unit tests for Phase 2 | missing |
| — | `a394d02` docs(02): verify Booking Core passed | missing |

Aggregate: 0 skill, 0 fallback, 0 exempt — 2 missing.

gate_status: skill=0, fallback=0, exempt=0, missing=2
