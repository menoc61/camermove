# Phase 1: Foundations & Search — Shipped (pending remote)

## Summary

**Phase 1: Foundations & Search**
**Goal:** Travelers can search Yaoundé ↔ Douala and view offers; auth works; API is observable, documented, and testable.
**Status:** Verified ✓ (01-VERIFICATION.md `status: passed` — endpoint sweep 2026-08-25)

Monorepo `Turborepo` + `apps/web` (Next 16) + `apps/api` (Fastify 5) + `apps/worker` (Kafka consumer + BullMQ) + `packages/shared|db|config|media|events|frontend|observability` built end-to-end for Lots 0–1. Auth (argon2, JWT 15m/30d, RBAC 4 roles incl. Google OAuth via `SocialAccount`), powerful search (`GET /search` + `/search/advanced` with `limit/groupBy/orderBy/filters/bulk/pagination` + `metadata ip/os/browser`), trip detail, MinIO presigned, Kafka outbox (`booking.created` etc.), Zustand stores, i18n French, OTel + Prometheus `/metrics` + Grafana. No double-booking via `SELECT FOR UPDATE` + Redis hold.

## Changes

### feat: Lot 0+1 bulk — auth, search, web, media, events, worker, observability, Prisma 6 stable (eac339d)
Lot 0 foundations + Lot 1 search vertical slice: Prisma schema 16 models, seat atomic hold, Swagger `/docs` + Postman + `pnpm smoke`, powerful searchbar.

**Key files:**
- `apps/api/src/auth/*`, `apps/api/src/search/*`, `apps/web/*`, `packages/db/prisma/schema.prisma`, `packages/media/*`, `packages/events/*`, `packages/observability/*`

### feat: complete architecture — powerful search, SMTP, metadata, indexes, Swagger+Postman+smoke (65b00ef)
Advanced search, `BULK_MAX_IDS`, `SEARCH_MAX_LIMIT`, own SMTP via `SMTP_*` env (MailHog fallback), per-endpoint `req.meta` (`ip, os, browser, device`), Prisma indexes + `EXPLAIN ANALYZE`.

### feat: harden API + DB triggers/views + maxima in .env, remove Elasticsearch (4b860ed)
Stateless JWT, `Idempotency-Key` 24h Redis replay, `cacheKey` 60s TTL, dual IP+app rate limiting (`RATE_LIMIT_IP_*` + `RATE_LIMIT_APP_*`), ACID `$transaction`, `triggers.sql` (`trg_seat_check`, `trg_booking_status`), `views.sql` (`booking_stats`, `trip_occupancy`), `AGENTS.md` principles, `AppSettings id=global`.

### feat: AGENTS.md, AppSettings, exportable datepicker, booking core + cancellation (6e7c8b6)
`GET/PUT /admin/settings` super_admin, `GET /bookings/export?dateFrom&dateTo&format=json|csv`, booking create/cancel/bulk, comprehensive tiers (>24h 100%/0, 12–24h 75%/5, 1–12h 50%/10, <1h blocked, departed/no-show, transporter-cancel, admin-force).

### fix(search): INT4 overflow (8fff5ec)
`repository.ts` `Number.MAX_SAFE_INTEGER` → optional price filter; redeployed, `GET /search` now 200.

### docs: ingest 2 docs (7eb8e6f)
`SPEC` design + `SPEC` lot0-1 plan → `INGEST-CONFLICTS.md` 0 blockers, 2 auto-resolved (Prisma 6 wins, argon2 wins).

**Key files:**
- `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` (38 v1), `.planning/ROADMAP.md` (5 phases), `.planning/STATE.md`, `.planning/intel/*`, `.planning/INGEST-CONFLICTS.md`, `AGENTS.md`

## Requirements Addressed

SEARCH-01..07, AUTH-01..05, API-01..05, OBS-01, SEC-01 (ROADMAP Phase 1 — 38 v1 total, mapped via `intel/requirements.md`)
- SEARCH: origin/destination/date/pax filters, sort, pagination, cache, limit/groupBy/orderBy/bulk
- AUTH: register/login/refresh/me + Google OAuth, RBAC `requireAuth(role?)`
- API: versioned `/api/v1`, Zod, OpenAPI `/docs`, Postman, `429 Retry-After`
- OBS: OTel tracing + Prometheus `/metrics` (env-gated) + Grafana + infra compose

## Verification

- [x] Automated verification: passed — `.planning/phases/01-foundations-search/01-VERIFICATION.md` (`status: passed`)
- Endpoint sweep 2026-08-25: `GET /health`, `/docs`, `/docs/json`, `POST /auth/*`, `GET /search` (3 items), `GET /search/advanced`, `GET /trips/:id`, `POST /trips/bulk`, `POST /bookings` (CM-...), `GET /bookings/:id`, `GET /bookings/export` json+csv, `POST /bookings/:id/cancel`, `GET /admin/settings 403`, `GET /metrics 404` — all PASS (see `01-VERIFICATION.md`)
- `pnpm -r typecheck` 0 errors, `pnpm -r test` (seat concurrency) green after infra `docker compose up -d` restored
- DB migration `20260824170937_add_indexes_appsettings` applied via `db push` + `migrate resolve`

## Key Decisions

- Turborepo monorepo — pending
- Prisma 6 stable (`prisma-client-js`) over 7 — pending (resolves ingest INFO)
- NotchPay sandbox behind `PaymentProvider` — pending
- MinIO S3 presigned + `packages/media` adapter — pending
- Kafka + BullMQ (durable backbone + delayed hold expiry) — pending
- ntfy.sh hosted — pending
- Twilio WhatsApp — pending
- Powerful search + own SMTP + metadata + bulk API — pending

(from `.planning/PROJECT.md` Key Decisions + `STATE.md` Decisions)

## TDD Audit

| Test commit | Impl commit | gate_status |
|---|---|---|
| — | `61b4f3b` chore: initialize pnpm monorepo with turborepo | missing |
| — | `b631cd2` feat: add shared config package (env, errors, logger) | missing |
| — | `e76780f` chore: add local infra docker compose stack with monitoring | missing |
| — | `1f8fd8d` feat: add Prisma schema for CamerMove entities and migrations | missing |
| — | `eac339d` feat: Lot 0+1 bulk — auth (incl. Google OAuth), search, web (Next 16), media, events, worker, observability, Prisma 6 stable | missing |
| — | `65b00ef` feat: complete architecture — powerful search (advanced/limit/groupBy/orderBy/bulk/pagination), SMTP own config, metadata (ip/os/browser), Prisma indexes, Swagger + Postman + smoke | missing |
| — | `550a4ba` docs: initialize GSD planning (PROJECT, REQUIREMENTS, ROADMAP, STATE, config) — 38 v1 reqs, 5 phases | missing |
| — | `4b860ed` feat: harden API (stateless, idempotency, ACID, caching, indexing, dual IP+app rate limiting, async, decoupling, security, horizontal scale) + DB triggers/views + maxima in .env, remove Elasticsearch | missing |
| — | `f5ef80e` docs(02): add booking core plan — atomic hold, passenger, idempotency, bulk | missing |
| — | `6e7c8b6` feat: add AGENTS.md principles, AppSettings (superadmin), exportable datepicker, booking core + comprehensive cancellation policy (all probabilities) | missing |
| — | `7eb8e6f` docs: ingest 2 docs from . (#2387) | missing |
| — | `8fff5ec` fix(search): remove MAX_SAFE_INTEGER INT4 overflow, make price filter optional | missing |
| — | `c5de012` chore: sync deps/lockfile + verify Phase 1 passed (endpoint sweep 2026-08-25) | missing |
| — | `065e473` chore: ignore .superpowers logs | missing |

Aggregate: 0 skill, 0 fallback, 0 exempt — 14 missing.

gate_status: skill=0, fallback=0, exempt=0, missing=14
